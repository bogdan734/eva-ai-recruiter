# Єва (AI-рекрутер Kozyr Trans) — інструкція для адмінів

Оновлено: 2026-08-07

## Що це і де живе

| Компонент | Де | Що робить |
|---|---|---|
| Сервер (VPS) | `ssh root@65.21.151.71`, код у `/opt/ai-recruiter` | все нижче крутиться тут у Docker |
| `api` | контейнер `deploy-api-1` | приймає події дзвінків від Vapi, аналізує розмови, шле анкети |
| `bot` | `deploy-bot-1` | Telegram-панель керування **@KozyrTransHRBot** |
| `scheduler` | `deploy-scheduler-1` | обдзвін по слотах **10:00 / 14:00 / 18:30** (Київ) + збір кандидатів: work.ua кожні 5 хв, robota.ua відгуки й чат кожні 15 хв |
| `tguserbot` | `deploy-tguserbot-1` | ТГ-акаунт Єви (@HR_KozyrTrans) — авто-відповіді кандидатам, відправка анкет |
| `db`, `caddy` | | база та веб-проксі — не чіпати |
| `scraper` | ЗУПИНЕНИЙ навмисно | старий, не потрібен — кандидати йдуть через work.ua API |
| Голос/дзвінки | vapi.ai, асистент `88ecfb7b-…` | мозок і голос Єви |
| Телефонія | Stream Telecom SIP, **бойовий номер +380 93 582 43 69** | вхідні + вихідні |

## Панель керування (для всіх адмінів)

Telegram → **@KozyrTransHRBot** → `/menu`. Доступ мають: Богдан, Тетяна, Світлана, Артем.

- ▶️ / ⏸ — запустити / поставити на паузу обдзвін
- 📞 — тестовий дзвінок на будь-який номер
- 🎯 — критерії відбору кандидатів (регіони, вік, поріг)
- 💼 — параметри вакансії
- 📨 Telegram Єва — керування ТГ-розсилкою (стоп/старт, ліміт, статистика)
- 📥 — ручний імпорт кандидатів

**90% питань вирішується з панелі. У сервер лізти — тільки якщо панель не допомогла.**

## Якщо щось зламалось (по черзі)

1. **Бот не відповідає в ТГ / Єва не дзвонить / не відповідає в ТГ-чатах:**
   ```bash
   ssh root@65.21.151.71
   cd /opt/ai-recruiter/deploy
   docker compose ps        # що впало? (має бути Up у всіх, крім scraper)
   docker compose restart bot          # або: api / scheduler / tguserbot
   ```
2. **Подивитись, що пише сервіс:**
   ```bash
   docker logs deploy-bot-1 --tail 50        # bot / api / scheduler / tguserbot
   ```
3. **Перезапуск усього (безпечно, ~30 сек):**
   ```bash
   cd /opt/ai-recruiter/deploy && docker compose restart
   ```
4. **Сервер завис повністю:** перезавантажити VPS у панелі Hetzner — після старту все піднімається саме.

## Дзвінки: пауза/запуск без панелі (аварійно)

Файл `/opt/ai-recruiter/state/ai_recruiter_state.json`:
`"calls_paused": true` — стоп, `false` — працює. Зміна діє одразу, рестарт не потрібен.

## Зміни голосу/скрипта Єви (тільки той, хто розуміє, що робить)

- Текст скрипта: `/opt/ai-recruiter/src/call/script_template.py`
- Після зміни ТІЛЬКИ тексту:
  ```bash
  cd /opt/ai-recruiter/deploy && docker compose build api bot scheduler && docker compose up -d api bot scheduler
  docker exec deploy-api-1 mkdir -p /app/scripts
  docker cp /opt/ai-recruiter/scripts/patch_eva.py deploy-api-1:/app/scripts/patch_eva.py
  docker exec deploy-api-1 python /app/scripts/patch_eva.py
  ```
- Голос/таймінги/привітання: `/opt/ai-recruiter/scripts/patch_eva.py` (потім ті самі 3 команди вище).

### Відкат швидкості реакцій (якщо Єва перебиває / обриває себе)

Зараз стоять ЕКСПЕРИМЕНТАЛЬНО мінімальні паузи. Повернути перевірені:
```bash
cp /opt/ai-recruiter/scripts/patch_eva.py.stable-20260720 /opt/ai-recruiter/scripts/patch_eva.py
docker cp /opt/ai-recruiter/scripts/patch_eva.py deploy-api-1:/app/scripts/patch_eva.py
docker exec deploy-api-1 python /app/scripts/patch_eva.py
```

### Відкат будь-якої зміни коду

Код під git: `cd /opt/ai-recruiter && git log --oneline` → `git checkout <хеш> -- шлях/до/файлу`, далі rebuild потрібного сервісу.

## Авто-анкета (нове з 20.07)

Якщо в дзвінку Єва пообіцяла надіслати анкету — після дзвінка ТГ-Єва сама пише кандидату
посилання на форму. Перевірити, що спрацювало:
```bash
docker logs deploy-api-1 2>&1 | grep anketa_form
```
Посилання на форму задається змінною `ANKETA_FORM_URL` у `/opt/ai-recruiter/.env`.

## Що НЕ робити

- Не міняти таймінги в patch_eva.py нижче за stable-значення без тестів.
- **Не прискорювати опитування robota.ua.** Інтервали й ліміти в `.env`
  (`ROBOTAUA_MAX_PAGES`, `ROBOTAUA_POLL_CRON_MINUTE`, `ROBOTAUA_MAX_CV_FETCH`)
  підібрані так, щоб не ловити Cloudflare. Побільшите — джерело замовкне на години.
- **Не перемикати транк `/trunk` «про всяк випадок».** Транки віддають РІЗНІ
  номери кандидату, це не резервні копії одного.
- Не видаляти файли `*.bak-*` і `patch_eva.py.stable-*` — це точки відкату.
- Не чіпати `db`, `caddy`, `.env` без розуміння.
- `docker compose up -d scraper` — НЕ запускати, він вимкнений свідомо.

---

## Оновлено 07.08.2026 — друга вакансія і що це змінило

### Тепер вакансій дві, і вони поводяться по-різному

| | Менеджер з продажу | **Бухгалтер** |
|---|---|---|
| дошки | work.ua 8249916, robota.ua 11277559 / 11284462 | work.ua 8242731 / 8374143, robota.ua 11249166 / 11292426 |
| воронка KeyCRM | 1 «Етап Менеджер з продажу» | **6 «Бухгалтер»**, етап «Новий» |
| Єва дзвонить / пише | так | **ні** |
| гео й вікові фільтри | так | **ні** — рекрутер дивиться сам |

**Бухгалтери ніколи не потрапляють у обдзвін.** Це не налаштування, а окремий
режим: такі кандидати одразу отримують статус «у рекрутера», а дзвонилка бере
тільки «нові» й «у черзі». Тому паузи/запуски обдзвону їх не стосуються.

Додати ще вакансію = один запис у `src/common/vacancies.py`. Більше нічого
правити не треба — воронка, етап, фільтри й дзвінки описані там же.

### Що варто знати про картки бухгалтера

- Поля «Вакансія», «Номер вакансії», «Опис вакансії», «Посилання» тепер видно й
  у цій воронці. Раніше значення писались, але не показувались — поле не було
  прив'язане до воронки в налаштуваннях KeyCRM.
- Контакт у картці **не зберігається покупцем** автоматично (як у менеджерських):
  видно «Контактні дані» й кнопку «Зберегти покупця». Рішення за рекрутером.
- ⚠️ У воронці 6 лежать **79 карток «ДУБЛЬ ВИДАЛИТИ»** на етапі «Не актуально» —
  це сміття від збою 06.08, позначене окремою назвою. Шукайте по слову
  `ДУБЛЬ ВИДАЛИТИ` і видаляйте пакетом. **Не видаляйте весь етап цілком** — там
  же лежать 5 нормальних карток.

### robota.ua працює повільніше, і це навмисно

Опитування зменшене з 10 до 15 хвилин, і за один прогін робиться вдвічі менше
запитів. Причина: Cloudflare банив нашу адресу на години, і 06.08 джерело
мовчало майже цілу добу. Після зміни — 23 прогони за 6 годин, один збій.

Якщо в `/status` бачите «пауза CF» — це не поломка, воно саме відновиться.
Черга запаркованих (зараз ~100) — це відгуки з прихованим телефоном.

### Резюме файлом тепер читаються

Якщо кандидат прикріпив резюме документом замість анкети (~8% на robota.ua),
телефон дістається з самого файлу — PDF, DOCX і навіть старий DOC. Раніше такі
люди були недосяжні. **Платні відкриття контактів на них не витрачаються.**

### Гроші рахуються чесніше

`/costs` тепер враховує не лише дзвінки, а й роботу Єви між ними — перевірку
імен на кожному новому кандидаті, оцінку відповідності, переписку в Telegram.
Раніше цих витрат у звіті не було взагалі.
⚠️ Токени самої розмови всередині Vapi нам не віддаються — це сліпа зона.

---

## Оновлено 21.07.2026 — що змінилось

**Робочий номер:** +380935824369 (Stream Telecom, бойовий). Тестовий 044 виведено з перемикача.

**Розклад тепер за київським часом** — 09/11/13/15/17/19. Раніше слоти помилково рахувались за UTC і спрацьовували на 3 години пізніше.

**Слот = повна сесія:** проходить усю чергу батчами по 3 дзвінки з паузою 2 хв, поки черга не спорожніє (раніше дзвонив лише 3 людям і зупинявся).

**AI-ліди помічені в CRM:** відповідальний — користувач «Єва АІ» (id 7, `KEYCRM_AI_MANAGER_ID` у .env). Так їх видно в списку серед звичайних карток.

**У картку CRM після кожного дзвінка пишеться:**
- AI Summary — 3 пункти підсумку
- AI Транскрипт — повний текст розмови
- AI Аудіо — посилання на запис
- стадія воронки змінюється автоматично

**Стадії воронки (pipeline 1):**
| Наш статус | Стадія в CRM |
|---|---|
| кваліфікований | Відібрано (2) |
| недодзвон | Недозвін (31) |
| у роботі / черзі | В роботі (3) |
| не підійшов | Не підходить нам (33) |

Змінити відповідність: `src/common/keycrm_fields.py` → `STAGE_MAP`.

**Віковий фільтр** працює на двох рівнях: Єва не запрошує поза вікном (Ж 23–42, Ч 23–40) + сервер перевіряє вік з розмови перед створенням ліда.

**Самолікування:** якщо картку видалили в CRM — система створить нову; якщо кандидат «завис» у статусі дзвінка — повернеться в чергу за 15 хв.

### Часті питання

**Єва замовкла / не дзвонить** → перевірити баланс Vapi та Anthropic. Це причина №1.

**Обдзвін не йде, хоча пауза знята** → `docker logs deploy-scheduler-1 --tail 30`, шукати `scheduler.batch` або `scheduler.empty_slot`.

**Ліда немає в CRM** → `docker logs deploy-api-1 | grep keycrm`. Система сама пересоздає видалені картки.

**Запустити обдзвін негайно, не чекаючи слоту:**
```bash
docker exec -d deploy-scheduler-1 sh -c "cd /app && PYTHONPATH=/app python /tmp/run_slot_now.py > /tmp/slot.log 2>&1"
```

---

## Оновлено 21.07.2026 (ніч) — Telegram-переписка тепер іде в CRM

**Раніше:** людина спілкувалась з Євою в Telegram, але рекрутер цього не бачив.

**Тепер:** Єва в чаті проводить той самий скринінг, що й по телефону. Хто підходить
і згоден на співбесіду → автоматично зʼявляється в CRM у стадії **Відібрано**,
відповідальний **Богдан Аі** (як позначка «оброблено АІ»). Хто явно не підходить →
**Не підходить нам**. У картці — підсумок розмови, транскрипт, регіон.

- ТГ-кандидати **не потрапляють у телефонний обдзвін** (окремий статус).
- Прихований номер у Telegram — не проблема, кандидат все одно потрапляє в CRM
  (внутрішній ключ за Telegram-ID). Якщо номер видно — картка обʼєднується з телефонною.
- На початку чату Єва кидає **посилання на вакансію** (work.ua/jobs/8249916).
- Гугл-форма (forms.gle/...) лишається для тих, хто **не хоче переписки** —
  але вона **не звʼязана з CRM**: відповіді йдуть у гугл-таблицю, рекрутер дивиться руками.

Змінити посилання на вакансію: `VACANCY_URL` у /opt/ai-recruiter/.env (rebuild bot+tguserbot).
