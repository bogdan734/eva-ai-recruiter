# HANDOFF — Єва AI-рекрутер (18.08.2026)

> **Точка входу для нового чату.** Тут стан і те, що робити далі.
> Історія й розбір кожного бага — `HANDOFF_2026-08-07.md` (секції 1–3e),
> глибше — `HANDOFF_2026-08-04.md`, `HANDOFF_2026-08-03.md`.
> Пам'ять: `project_ai_recruiter`.
>
> VPS `ssh root@65.21.151.71`, код `/opt/ai-recruiter`, деплой `deploy/docker-compose`.
> **Код вшивається в образ на build.** Після зміни `src/` обов'язково
> `docker compose build <service> && docker compose up -d <service>`.
> `/opt/ai-recruiter` у контейнер НЕ змонтований.

---

## 0. ЩО РОБИТИ ПЕРШИМ

Мета: **прибрати дублі повністю**, лишивши один шлях — наш.

### Крок 1 — догнати пропущені відгуки (чекає рішення клієнта)

Курсор work.ua проскочив ~212 відгуків на дві вакансії, поки вони не були
заведені (див. §2). Щоб їх забрати:

```bash
# відмотати курсор назад; дедуп по телефону, повторів не буде
ssh root@65.21.151.71
python3 -c "
import json; p='/opt/ai-recruiter/state/workua_cursor.json'
d=json.load(open(p)); print('було', d)
d['responses_last_id']=539200000   # ~2 тижні назад, підібрати по даті
json.dump(d, open(p,'w'), indent=2); print('стало', d)
"
```

⚠️ Разово створить до **двохсот карток** у CRM. Це очікувано (клієнт скаржився,
що не бачить бухгалтерів), але зміна помітна — **узгодити перед запуском**.
Бухгалтери — intake-only, дзвінків не буде. Продажі підуть у чергу обдзвону.

### Крок 2 — переконатись, що наш потік зрівнявся з чужим

День-два спостереження. Порівнювати:

```bash
# наші картки за добу
docker compose exec -T db psql -U recruiter -d recruiter -c \
  "select count(*) from candidates where created_at > now() - interval '1 day';"
# їхні за той самий час — картки у воронці 1 з manager_id != 7
```

Орієнтир: чужа інтеграція давала ~80 карток/добу з work.ua. Поки ми даємо
менше — **ключ не чіпати**.

### Крок 3 — прибрати дублі (оновити API-ключ KeyCRM)

Відповідь підтримки KeyCRM (14.08): **ключ один на всі інтеграції**. Оновлення
ключа видаляє старий назавжди.

1. Взяти новий ключ: Налаштування → Основні → API ключ → кнопка оновлення
   (логін у файлі доступів; або відкрити браузером користувача).
2. Вписати в `/opt/ai-recruiter/.env` → `KEYCRM_API_TOKEN=...`
3. `cd deploy && docker compose up -d` (build не потрібен, це env)
4. Перевірити: `GET /pipelines` → 200, і що картки Єви створюються.

Після цього чужа інтеграція (IP **91.206.200.150**) мертва. Дублі зникають.

⚠️ Разом із нею зникнуть канали, яких у нас немає. За 6 тижнів вони дали:
**Анкети 55**, **Telegram 3**. Facebook / єРобота / E-mail — **нуль**, це історія.
Тобто втрата мінімальна, але «Анкети» (~9/тиждень) варто проговорити з клієнтом.

---

## 1. Стан на 18.08 09:17 UTC

| | |
|---|---|
| Дзвінки | увімкнені (`calls_paused: false`) |
| Дзвінків за 3 дні | **0** — черга була порожня через мертвий інтейк (полагоджено) |
| Черга | `in_call_queue` 3, `manager_review` 102, `closed` 127 |
| Лінія | працює; 5 номерів у чорному списку, останній збій 14.08 |
| Курсор work.ua | `responses_last_id: 539438740` |
| Гілка | `work/2026-08-multi-vacancy`, HEAD `b70375d`, tree чистий |

---

## 2. 🔴 ГОЛОВНЕ ЗА 18.08: наш work.ua-інтейк був мертвий і мовчав

За 6 тижнів: чужа інтеграція з work.ua — **3452** картки, наша — **145**.

**Дві причини, обидві полагоджені (коміт `b70375d`).**

### Поштучний ендпоінт віддає 404, а клієнт ковтає 404
`poll_responses` ходив `GET /jobs/{id}/responses/` по кожній вакансії. Той
ендпоінт для наших вакансій віддає **404**, а `list_responses_for_vacancy`
перетворює 404 на порожній список. Результат: `new=0 errors=0` щопрогону,
тижнями, без єдиної помилки в логах.

Тепер завжди загальний фід по акаунту (`GET /jobs/responses/`) + маршрутизація
за `job_id`. Він працює.

### work.ua міняє id вакансії при перепублікації
Вшиті id тихо застарівають **за задумом платформи**. Дві живі вакансії не були
заведені взагалі — це **74% потоку**:

| job_id | відгуків | хто відгукується | заведено як |
|---|---|---|---|
| 8409676 | 156 | суцільно бухгалтери | `accountant` |
| 8346465 | 56 | продажі, логістика | `sales` |

⚠️ Визначено **за резюме тих, хто відгукнувся**, не за документом. Варто
перепитати клієнта, чи збігається.

Заведені через `vacancy_store` (файл `state/vacancy_overrides.json`), не в коді —
тобто видно й редагується з панелі: `/menu → 💼 Параметри вакансії → вакансія →
⚙️ Збір і обдзвін`.

### Тепер невідома вакансія не зникає тихо
Раніше відгук на незнайомий `job_id` відкидався одним `info`-рядком. Тепер
рахуються за прогін і пишеться **WARNING** із переліком:

```
workua.unknown_vacancies  jobs={8409676: 7}  skipped=7
```

**Це головний сигнал, що вакансію перепублікували.** Побачив — заведи новий id
у панелі.

---

## 3. Дублі в KeyCRM — повна картина

**Чужа інтеграція:** зовнішня, IP `91.206.200.150`, крон кожні 15 хвилин
(`:00:04`, `:15:03`, `:45:04`). Автор на зв'язок не виходить. У налаштуваннях
KeyCRM її немає (перемикач *Neuron integration* вимкнений — це не вона).

**Її почерк на картці:** `manager_id 3` (Svitlana), коментар
`Telegram: …; Viber: …`, поле «Вакансія» завжди `Менеджер з продажу`,
«Номер вакансії» і «Посилання» порожні.

**Скільки її:** воронка 1 містить 8455 карток, з них наших **145**.
За 6 тижнів вона дала 3609 карток, з яких **98.4% дублюють те, що тягнемо ми**
(work.ua 3452 + rabota.ua 99).

**Що з нею можна:**

| дія | можливо? |
|---|---|
| видалити картки | ❌ немає в API (`405`), немає в UI |
| перенести картку в іншу воронку | ✅ через `PUT {"status_id": <статус потрібної воронки>}` — **не** `pipeline_id` |
| вимкнути інтеграцію вибірково | ❌ ключ один на все |
| вимкнути оновленням ключа | ✅ див. §0 крок 3 |

⚠️ **KeyCRM застосовує зміну із затримкою кілька секунд.** Перевіряти результат
одразу після `PUT` — хибний висновок; чекати 4–5 с. Я на цьому вже помилився і
двічі зробив неправильний висновок «перенос не працює».

⚠️ **KeyCRM відповідає `202 {"status": true}` навіть на те, що ігнорує.**
Успішна відповідь ≠ зміна застосована. Завжди перечитувати об'єкт.

---

## 4. Пастки, на яких найлегше зламатись

Повний список — `HANDOFF_2026-08-07.md` §2. Найгостріші:

- **Fail-silent — головний ворог цього проєкту.** Три рази поспіль система
  «працювала» з нульовим результатом: дедуп по телефону, probe robota.ua,
  тепер work.ua-інтейк. Якщо лічильник = 0 і помилок = 0 — це підозра, не норма.
- **Не fail-open на дедупі.** Помилка перевірки = «пропусти», а не «створюй».
- **Лічильники у фіналізації дзвінка мусять бути ідемпотентними.** `reconcile`
  переобробляв ті самі дзвінки кожні 10 хв і накрутив брейкер до хибної паузи.
- **Не прискорювати опитування robota.ua.** 44 запити/год — вистраждана межа,
  вище ловиться Cloudflare на години.
- **Числа в промпті — словами.** Єва їх вимовляє: `25000` → «двадцять п'ять
  тисяч». Латиниця читається англійською.
- **Автоматика має звужувати шкоду, а не вимикати систему.** Перша версія
  брейкера зупиняла весь обдзвін і хибно спрацювала на трьох номерах.

---

## 5. Де що лежить

| | |
|---|---|
| Інструкція клієнту | `~/Desktop/EVA_ІНСТРУКЦІЯ.md` + https://claude.ai/code/artifact/f7bf0ee6-93ff-49de-9a0c-d93966ec596a |
| Доступи (паролі) | `~/Desktop/EVA_ДОСТУПИ.md` — права 600, **не пересилати** |
| Варіанти по інтеграції | `KEYCRM_ІНТЕГРАЦІЯ_ВАРІАНТИ.md` |
| Звернення в підтримку | `KEYCRM_ЗВЕРНЕННЯ_ПІДТРИМКА.md` (вже відправлене, відповідь у §3) |
| Лист оператору | `STREAM_TELECOM_403_12-08.md` |
| Бриф Софії | `~/Desktop/tg-recruiter/BITRIX_SOFIA_BRIEF.md` — секція **B2d** = що вже готове |

**Клієнту про репозиторій не казати** — у документах для нього згадок немає.

---

## 6. Відкрите, крім дублів

- **Stream Telecom**: кабінету в нас немає, лише SIP. Баланс і канали не видно,
  тому збої діагностуємо наосліп. Лист із доказами готовий.
- **Автопоповнення Anthropic вимкнене** — скінчаться кошти, Єва стане.
  Витрати ~$0.06/добу, запасу надовго, але поповнення ручне.
- **Баланси в стані від 03.08** — вписані руками, реального залишку не показують.
- **6 областей у гео-фільтрі** чекають рішення клієнта.
- **71 картка «ДУБЛЬ ВИДАЛИТИ»** у воронці 6 — видалити в KeyCRM неможливо
  взагалі; лежать на прихованому етапі й нікому не заважають.
