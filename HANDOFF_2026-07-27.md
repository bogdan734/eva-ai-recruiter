# HANDOFF — Єва AI-рекрутер (2026-07-27)

> Читати РАЗОМ з памʼяттю `project_ai_recruiter` + HANDOFF_2026-07-24.md.
> Все в бою. VPS `ssh root@65.21.151.71`, код `/opt/ai-recruiter`, деплой `deploy/docker-compose`.

## Що виправлено сьогодні (все задеплоєно, webhook 200, 6 сервісів Up)

1. **Єва мовчала в ТГ 3 дні (24→27.07)** — декоратор `@client.on(NewMessage)` стояв над
   `_push_progress` замість `on_message`. Перенесено. + `catch_up_unread()` на старті
   (env `TG_CATCHUP_ON_START=1`), ручний `POST :8090/catchup?dry=1&only=peer1,peer2`.
2. **Одеса помилково відсіювалась** — гео жило в 4 місцях і розходилось; у промптах була
   абстракція «правобережна Україна», модель віднесла Одесу до лівого берега.
   Тепер ЯВНИЙ СПИСОК 13 областей (12 + Одеська) скрізь: .env REGION_WHITELIST,
   settings.py, script_template.py, summarizer.py, tg _CLASSIFY_SYSTEM, persona.py.
   **УРОК: ніколи не давати моделі географічну абстракцію — тільки перелік.**
3. **Застряглі в Недозвоні** — `store.outcomes` було 0 записів (вердикт не виставлявся
   ніколи). Прогнано 12 діалогів, 7 термінальних застосовано.
4. **Порожній дзвінок затирав реальну розмову** — мертва спроба (0с) фіналізувалась
   ПІСЛЯ успішної: стирала картку, кидала в Недозвін, слала «не змогли додзвонитися»
   людині яку щойно скринили. Guard у `_finalize_call` (`empty_call_ignored`).
5. **Стадії рекрутера перебивались** — `handle_tg_outcome` тепер читає ЖИВУ стадію і
   НЕ рухає картку, якщо там уже рішення людини (лог `tg_outcome.stage_kept`).
6. **Адміни** (`TG_ADMIN_CHAT_IDS`) виключені зі скринінгу — Єва «співбесідувала» Тетяну.
7. **ТГ-персона не розкриває критерії** (правило було лише в голосі) + без прізвища.
8. **PRIVACY_PREMIUM_REQUIRED** не ловився → ронив HTTP-запит. Виправлено +
   персистентний лічильник: `GET :8090/outreach_stats?days=30`.

## 🔴 ВІДКРИТЕ
1. **Максимчук Оля (картка 9554)** — я помилково повернув у «В роботі»; рекрутер ставив
   іншу стадію 22.07. ЧЕКАЮ від клієнта куди повернути (KeyCRM history API = 404).
   Хохлова 9539 уже відкочена в «Не ЦА» + candidate 3125 closed.
2. **Stream Telecom** — 27.07 ОБИДВА дзвінки `failed-to-connect`. Перевірити БАЛАНС
   у crm.streamtele.com. Vapi/Anthropic/KeyCRM/VPS — гроші є, все 200.
3. **Telegram Premium** — заміряємо через outreach_stats тиждень, потім рішення.
4. **ElevenLabs $22/міс — НЕ поновлювати**, голос іде через Vapi (перевірено голосом).

## Backups 2026-07-27
`service.py.bak-20260727-handler|-catchup|-outreachlog`, `persona.py.bak-20260727-geo`,
`*.bak-20260727-geo13` (5 файлів), `tg_outcome.py.bak-20260727-namededup|-stageguard`,
`orchestrator.py.bak-20260727-emptycall`, `.env.bak-20260727-catchup|-geo13`.

---

## robota.ua — розвідка кабінету 2026-07-28 (доступ через браузер клієнта)

**Пріоритет клієнта:** Відгуки → 24 чати → потім думаємо.
**🔴 РЕЖИМ: ТІЛЬКИ CRM ДЛЯ ТЕСТІВ.** Завтра-післязавтра у клієнта інтервʼю.
Дзвінки/розсилку по robota.ua НЕ вмикати до окремої відмашки.

**Кабінет (акаунт Світлана, KozyrTrans):**
- Вакансії: `robota.ua/my/vacancies`
- Кандидати: `robota.ua/my/vacancies/{vacancy_id}/candidates`
- Активні: **11277559** «Менеджер з логістики (B2B продажі)» Дніпро 30-65к —
  37 кандидатів (4 нових) / 99 рекомендованих;
  «Менеджер з логістики та продажу транспортних послуг (Junior)» Дніпро 25-40к —
  28 кандидатів (7 нових) / 97 рекомендованих.
- Завершена «Менеджер з логістики (B2B продажі)» — 35 кандидатів, **14 непереглянутих**.
- Вкладки: «Кандидати» (= відгуки, безкоштовно) vs «Рекомендовані» (= холодна база,
  відкриття контактів платне). Фільтр «Тільки з відкритими контактами».

**🔴 24 НЕПРОЧИТАНІ ЧАТИ** (віджет унизу справа, найстаріші з 09.05.2026):
Несен Антон 27.07(2), Настасяк Лідія 21.07(1), Bondar Lera 14.07(1),
Bohdana Tsipar 03.07(1), Левчунець Євгенія 09.05(2) + ще. Люди пишуть змістовно
(«маю досвід у службі підтримки», «стажування на логіста») і чекають місяцями.
Відкритого API в чатів немає — лише через браузер (ризик як з TG-юзерботом).

**Спостереження:** дві активні вакансії мають схожі назви → кандидати плутають
(клієнт: «відволікаються на менеджера з транспортної логістики»).
Перші ж кандидати — Київ (Золотухін, Сазонова 50р) → після звуження гео до 11
областей відсіються.

**Блокер для авто-інтеграції:** `robotaua_api.py` = Playwright-скрапер, потребує
`ROBOTAUA_EMPLOYER_EMAIL/PASSWORD` у .env. Паролі не передаються — або клієнт
вписує сам, або разовий збір через браузер.

## 🔴 БАГ ВІДТВОРЕНО НА ТЕСТІ 2026-07-29: картка НЕ створюється для кваліфікованого

**Тест robota.ua E2E** (кандидат 3170, `+380670000001`, source=robotaua):
1. `InboundRouter.ingest()` → OK: accepted=True, reason=`deferred_until_qualified`,
   candidate_id=3170, картки нема (за дизайном).
2. `_finalize_call()` з транскриптом кваліфікованого (Львів, 30р, 2р4м досвіду B2B,
   готова на повну зайнятість, дата звільнення) →
   **статус=manager_review, регіон=Львівська, keycrm_lead_id=None** ❌

Тобто `should_push` (summary.qualified=True) не призвів до `create_lead` — у логах api
жодного create для цього кандидата. Це ТОЙ САМИЙ баг, що загубив картку **Єфімової
(3156)**: реальна розмова 63с, `manager_review`/closed, картки немає.
**Раніше вважав одиничним збоєм — тест доводить, що системний.**

НАСТУПНИЙ КРОК: пройти `_finalize_call` покроково по гілці
`if not candidate.keycrm_lead_id: should_push = ...` — перевірити, чи блок взагалі
досягається (можливо, ранній `return`/виняток вище, або сесія відкочується).
Тест-скрипти: `/tmp/test_robota.py`, `/tmp/test_r2.py` на VPS (у deploy-api-1).

**ЩО ПРАЦЮЄ по robota.ua:** джерело зареєстроване (`robotaua` → label «robota.ua»,
вимова «робота юей», origin «Ви залишали резюме на робота юей»), інтейк приймає
`work_ua_url`/`resume_text`/`region_raw`/`experience_years`, гео-фільтр по 11 областях,
дедуп по телефону. Скрапер відгуків НЕ увімкнений (треба креденшели в .env).

⚠️ ТЕСТ-АРТЕФАКТ: кандидат **3170** «🧪ТЕСТ Робота Кандидат Тестович» у БД (картки нема).

---

## 🔴🔴 ГОЛОВНИЙ БАГ СЕСІЇ ЗНАЙДЕНО Й ВИПРАВЛЕНО 2026-07-30 (orchestrator.py.bak-20260729-indent)

**ПОМИЛКА ВКЛАДЕНОСТІ: 130 рядків (628-757) сиділи ВСЕРЕДИНІ `if needs_tg_outreach:`.**
Тобто ВСЯ післядзвінкова обробка — `create_lead`, `assign_manager`,
`write_call_results` (транскрипт/аудіо/вік/місто/резюме), `move_to_status`,
buyer-нотатка, блок «ЩО ЗРОБЛЕНО» — виконувалась ТІЛЬКИ для тих, кому слався
TG-outreach (тобто для недозвонів).

**КВАЛІФІКОВАНИЙ КАНДИДАТ НЕ ОТРИМУВАВ КАРТКИ ВЗАГАЛІ** — `needs_tg_outreach=None`
→ блок пропускався. Це пояснює Єфімову (63с розмови, картки нема) і чому «недозвони»
картки мали, а найцінніші — ні.

Фікс: дедент блоку на 4 пробіли. Перевірено `diff` зі знятими відступами —
**логіка не змінена, тільки рівень вкладеності**. E2E-тест після фіксу:
кандидат зі Львова (30р, 2р4м B2B) → картка **9793**, стадія 3 В роботі, manager 7,
client_id 4425 (зелена галочка), заповнені Вакансія/Номер/AI Аудіо/AI Транскрипт/
AI Summary/AI Регіон/Вік/Місто/Посилання на резюме (robota.ua лінк). ВСЕ на місці.

**НАСЛІДКИ: 23 реальних кандидати без карток** (розмови 48-316с!), з них 6
кваліфікованих (Каназірська 256с, Кисіль 247с, Аксьонова 242с, Елистратова 316с,
Дубовик 151с, Галаса 110с). Відновлено скриптом `/tmp/recover.py` (картки 9794-9810,
у нотатці «⚠️ картка ВІДНОВЛЕНА»). Скрипт таймаутнув на 2хв — **ПЕРЕВІРИТИ чи всі 23
пройшли** (`SELECT ... WHERE keycrm_lead_id IS NULL AND EXISTS(calls>=30s)`).

⚠️ **ПИТАННЯ КЛІЄНТА (валідне, не закрите до кінця):** чи не видаляв рекрутер ці
картки раніше — тоді відновлення = дублі. Аргумент проти: `keycrm_lead_id` у коді
НІДЕ не скидається в NULL (grep порожній), тобто видалена картка лишила б ID.
Але сценарій «картка створилась, запис ID відкотився» не виключений. KeyCRM API не
дає ні пошуку по імені, ні фільтра по телефону, ні сортування -id → перевірка лише
в UI. ЯКЩО ДУБЛІ: видаляти МОЮ копію (мітка «ВІДНОВЛЕНА»), lead_id повернути на оригінал.

**УРОК (2 рази за сесію): перед масовою зміною в CRM — спершу порахувати й показати
клієнту, потім робити.** Так само було зі стадіями Хохлової.

## ПОВНА ПОТУЖНІСТЬ з 2026-08-03 (клієнт)
Піднято: `CALL_MAX_CONCURRENT` 5→**8**, TG `MAX_NEW_CONTACTS_PER_DAY` 15→**25**
(ліміт TG живе у STATE, не в env → міняти через `POST :8090/limit`).
Слоти лишились 10:00/14:00/18:30. Backup `.env.bak-20260802-fullpower`.

**🔴 БЛОКЕРИ ГРОШЕЙ (клієнт обіцяв закинути):** Vapi **$9.96** (~75 розмов = 1-2 дні
на повній потужності), Anthropic **$17.98** з **вимкненим auto-reload** (закінчиться →
система стане ПОВНІСТЮ). Картка Mastercard ••••8407 вже не пройшла для ElevenLabs —
якщо проблема в ній, автопоповнення не спрацює на обох.
